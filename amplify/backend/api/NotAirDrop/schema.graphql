type SendURL @model @auth(rules: [{allow: public}]) {
  id: ID!
  fromUID: String!
  toUID: String!
  fileURL: String!
}

type UIDS @model @auth(rules: [{allow: public}]) {
  id: ID!
  uid: String!
}
 